// The examples run in the browser against the IIFE build, which exposes the
// library as the `kaplay` global. This makes // @ts-check work in editors.
declare const kaplay: typeof import("../src/index");
